let baseNumber=3;
let power=5;
let answer=calculatePower(baseNumber,power);
console.log(`${basenumber} to the power of${power} is ${answer}`);

function calculatePower(number,pow ){

    return number**pow;
    
}